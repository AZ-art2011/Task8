// Вывод четных чисел в указанном диапазоне

Console.WriteLine("Введите число - границу диапазона");
int limit = Convert.ToInt32(Console.ReadLine());

for (int t=1; t < limit; t++){
    if t % 2 == 0{
        Console.WriteLine(t)
    }
}
